build/
*.egg-info/
__pycache__/
*.pyc
*.pyd
*.so
dist/
# ...but the compiled plugin artifacts ARE committed: RhinoCode (licensed
# Rhino) is the only thing that can build compas_wood.rhp, so CI packages the
# committed copy into the Yak release (see publish-yak.yml).
!plugin_rhino/plugin/dist/
!plugin_rhino/plugin/dist/**
generated
*.3dmbak
*.rhl
*.mtl
*.pb
serialization/
mesh_offset_test_dump.json
*.ai
src/wood_nano/_build_info.py
